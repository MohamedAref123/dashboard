import { ClaimTranslatePipe } from './claim-translate-pipe';

describe('ClaimTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
